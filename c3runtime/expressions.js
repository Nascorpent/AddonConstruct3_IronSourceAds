"use strict";

{
    self.globalThis.C3.Plugins.Nascorpent_IronSourceAds.Exps = {


        lastPlacementId () {
            //console.log(self.TAG + " Exps " + "lastPlacementId");
            return this.lastPlacementId
        },

        lastAdInfo () {
            //console.log(self.TAG + " Exps " + "lastAdInfo");
            return this.lastAdInfo
        },

        lastPlacementId_Error () {
            //console.log(self.TAG + " Exps " + "lastPlacementId_Error");
            return this.lastPlacementId_Error
        },

        lastErrorCode () {
            //console.log(self.TAG + " Exps " + "lastErrorCode");
            return this.lastErrorCode
        },

        lastErrorMessage () {
            //console.log(self.TAG + " Exps " + "lastErrorMessage");
            return this.lastErrorMessage
        }
    }
}
